# เริ่มงานต่อจากตรงนี้

> ไฟล์สั้นสำหรับเปิดการสนทนาใหม่ · รายละเอียดเต็มอยู่ใน [PROJECT_STATUS.md](PROJECT_STATUS.md)

## โปรเจกต์นี้คืออะไร

**Life Competency UP TO GROW** — ระบบจัดกิจกรรมโฮมรูมพัฒนาทักษะชีวิตและความเป็นพลเมืองดี
พร้อมวัดผลก่อน–หลัง วิเคราะห์สถิติ และแดชบอร์ด 3 ระดับ (นักเรียน / ครู / ผู้บริหาร)

- เว็บใช้งานจริง https://sarawut2206.github.io/LIFECRAFT-AI/
- ไฟล์งานจริง `index (30).html` (ไฟล์เดียวจบ ไม่มี build step)
- ผู้พัฒนา นายศราวุธ พิมศร · โรงเรียนมัธยมวัดหนองแขม

## งานถัดไปที่ควรทำก่อน

**🔴 Symposium ศุกร์ที่ 14 ส.ค. 2569** (เวทีแลกเปลี่ยน ไม่ใช่ประกวด)
ยังไม่มีสื่อนำเสนอเลย ควรทำตามลำดับนี้

1. แผ่นพับ 1 หน้า + QR ให้ครูสแกนลองใช้ในห้องประชุมทันที ← คุ้มที่สุด
2. สคริปต์นำเสนอจับเวลา (ยังไม่ทราบว่าได้กี่นาที ต้องถามผู้จัด)
3. คู่มือการนำไปใช้ระดับโรงเรียน สำหรับผู้บริหารโรงเรียนอื่น

**🟡 ค้างอยู่**
- ผู้เชี่ยวชาญ 5 ท่านประเมิน IOC จริง (ค่าปัจจุบันเป็นข้อมูลจำลอง)
- เก็บข้อมูลจริงให้ครบ (ตอนนี้ 6/15 คน)
- **ให้ครูประเมินแบบสังเกตพฤติกรรม 9 ด้าน กับนักเรียนจริงอย่างน้อย 3 คน**
  ระบบจึงจะคำนวณค่า r ในตารางที่ 6 ได้ (ทำได้ที่หน้าครู → ดูรายคน)
  **ด้านที่ 3 และ 4 ของหน้าหลักฐานจะขึ้น "รอข้อมูล" จนกว่าจะทำข้อนี้**
- **ชวนนักเรียนกลับมารายงานผลการลงมือทำ** การ์ดขึ้นเองบนหน้าแรกหลังทำภารกิจเสร็จ
  แล้วครูกดยืนยันหลักฐานในหน้าหลักฐานความสำเร็จ — เป็นหลักฐานด้านที่ 5 ที่ยังว่างอยู่ทั้งหมด
- ปรับรายงาน 5 บทให้ตรงระบบปัจจุบัน (ยังเขียนว่า 7 ด้าน 10 ข้อ)
- รัน `migratePulseRows()` ใน Apps Script หนึ่งครั้ง

## 4 เรื่องที่ต้องระวังเมื่อแก้โค้ด

1. **แก้ที่ `index (30).html`** ตอน push คัดลอกเป็น `index.html`
2. **ตรวจ syntax ทุกครั้ง** ดึง `<script>` ออกมา `node --check`
   แล้วตรวจ inline handler ด้วย `new Function()` (handler จาก template พังตอนคลิกเท่านั้น)
3. **แก้ `sw.js` ต้องเพิ่ม `VERSION`** ตอนนี้อยู่ที่ `v8` ครั้งถัดไปเป็น `v9`
   ไม่งั้นเครื่องที่ติดตั้งแอปแล้วไม่ได้ไฟล์ใหม่
4. **มีรูบริก 2 ชุด อย่าสลับกัน**
   `RUBRICS` = นักเรียนประเมินตนเอง 4 ด้าน · `TOBS` = ครูสังเกตพฤติกรรม 9 ด้าน

## สิ่งที่ห้ามลืม

- ข้อมูลเดิม 6 คนทำแบบวัด **10 ข้อ** ฉบับปัจจุบัน **12 ข้อ** — คะแนนรวมเทียบกันไม่ได้
  (ระบบเตือนอัตโนมัติแล้ว) แต่คะแนนรายด้านเทียบได้
- ระดับที่ครูเคยให้ไว้ในรูบริกเดิมจะขึ้นเป็น **3/9 ด้าน** เพราะคีย์ `self` `think` `comm`
  ใช้ต่อได้ ส่วนคีย์ `civic` เดิมถูกแยกเป็น 6 ด้านย่อย ระบบไม่เดาค่าให้
  แต่แสดงเป็นแถว "ค่าจากแบบสังเกตฉบับเดิม" ไว้ให้ครูดูประกอบ
- ตัวเลขในรายงานบทที่ 4 เป็น **ข้อมูลจำลอง** มีหมายเหตุกำกับไว้ อย่านำไปอ้างว่าเป็นผลจริง
- ผู้บริหารเห็นเฉพาะ **สรุประดับห้อง ไม่มีคำตอบรายบุคคล** (ออกแบบไว้เพื่อ PDPA)
