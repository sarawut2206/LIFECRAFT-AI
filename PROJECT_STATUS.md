# สถานะโปรเจกต์ Life Competency UP TO GROW

> **Checkpoint 13 สิงหาคม 2569** · สรุปสถานะทั้งหมดของโปรเจกต์ ณ วันนี้
> ใช้เป็นจุดตั้งต้นเมื่อเปิดการสนทนาใหม่ อ่านไฟล์นี้ก่อนเริ่มงานต่อ
> ไฟล์สั้นสำหรับเริ่มงานเร็ว ดูที่ [NEXT_STEPS.md](NEXT_STEPS.md)

---

## 0. สถานะ ณ จุด checkpoint

| | |
|---|---|
| โค้ดล่าสุด | งานวันที่ 13 ส.ค. ทั้งหมด · `sw.js` = `v17` |
| ไฟล์ในเครื่องกับใน repo | **ตรงกันทุกไบต์** ทั้ง `index.html` และ `sw.js` · working tree สะอาด |
| ขนาดโปรแกรม | 608,335 bytes (391,178 ตัวอักษร) ไฟล์เดียวจบ |
| เว็บใช้งานจริง | https://sarawut2206.github.io/LIFECRAFT-AI/ (deploy แล้ว ตรวจสอบแล้ว) |
| หน้าจอในระบบ | 36 มุมมอง · แยกเป็น 3 แอปตามบทบาท |
| ⚠️ เรื่องด่วนที่สุด | **Symposium พรุ่งนี้ 14 ส.ค.** — ยังไม่มีสื่อนำเสนอเลยแม้แต่ชิ้นเดียว |

---

## 1. ภาพรวม

**ผู้พัฒนา** นายศราวุธ พิมศร · ครู วิทยฐานะชำนาญการ
กลุ่มสาระการเรียนรู้คณิตศาสตร์ โรงเรียนมัธยมวัดหนองแขม · สพม.กทม. เขต 1

**นวัตกรรม** ระบบจัดกิจกรรมโฮมรูมเชิงสถานการณ์ พัฒนาทักษะชีวิตและความเป็นพลเมืองดี
พร้อมระบบวัดผลก่อน–หลัง วิเคราะห์สถิติ และแดชบอร์ด 3 ระดับ (นักเรียน / ครู / ผู้บริหาร)

**เผยแพร่** https://sarawut2206.github.io/LIFECRAFT-AI/
**Repo** https://github.com/sarawut2206/LIFECRAFT-AI (public)

**ภายใต้โครงการ** สร้างและส่งเสริมความเป็นพลเมืองดีตามรอยพระยุคลบาทด้านการศึกษาสู่การปฏิบัติ
สำนักงานศึกษาธิการจังหวัดกรุงเทพมหานคร / ศธภ.2

---

## 2. งานที่ต้องทำต่อ เรียงตามความเร่งด่วน

### 🔴 ด่วนที่สุด — Symposium **พรุ่งนี้** ศุกร์ที่ 14 สิงหาคม 2569

หนังสือเชิญ ที่ ศธ 0257/ว548 ลงวันที่ 8 ส.ค. 2569 เชิญ **นายศราวุธ พิมศร** โดยระบุชื่อ

| | |
|---|---|
| วัน–เวลา | ศุกร์ที่ 14 ส.ค. 2569 · 08.30–16.30 น. |
| สถานที่ | โรงแรมดิไอเดิล โฮเทล แอนด์ เรสซิเดนซ์ จ.ปทุมธานี |
| กลุ่ม | **กลุ่มที่ 1 สมุทรปราการ + กรุงเทพมหานคร** |
| วิทยากรกลุ่ม | นายเทพฤทธิ์ ยอดใส ผอ.ร.ร.ราชวินิตมัธยม (ผอ.เชี่ยวชาญ) |

**นี่ไม่ใช่การประกวด** แต่คือเวทีแลกเปลี่ยนเพื่อ **ต่อยอดนวัตกรรม · ถอดบทเรียน · สร้างรูปแบบ/แนวทาง**
งานส่งประกวดรอบ 7 ส.ค. ผ่านไปแล้ว

**กำหนดการ**
- 09.30–12.00 เสวนาวิชาการ + นำเสนอรูปแบบ/แนวทาง (แบ่ง 3 กลุ่มจังหวัด)
- 13.00–15.00 Symposium แลกเปลี่ยนเรียนรู้
- 15.00–16.30 สรุปการนำรูปแบบไปปฏิบัติในพื้นที่

**สิ่งที่ยังไม่มี — เหลือเวลาไม่ถึงหนึ่งวัน เลือกทำเท่าที่ทัน**
- [ ] แผ่นพับ 1 หน้า + QR ให้ครูสแกนลองใช้ในห้องประชุมทันที ← **คุ้มที่สุด ถ้าทำได้แค่ชิ้นเดียวให้ทำอันนี้**
- [ ] สคริปต์นำเสนอจับเวลา (ยังไม่ทราบว่าได้กี่นาที ต้องถามผู้จัด)
- [ ] คู่มือการนำไปใช้ระดับโรงเรียน สำหรับผู้บริหารโรงเรียนอื่น

> **จุดขายที่ควรเล่าบนเวที** ไม่ใช่ตัวเลขคะแนน แต่คือ
> (ก) กลไกตรวจสอบความตรง 3 ชั้นที่จับ social desirability bias ได้จริง 2 กรณี (ดูข้อ 5)
> (ข) ระบบภารกิจต่อเนื่อง 4 ชั้น วัน→สัปดาห์→เดือน→เทอม ที่ครูโรงเรียนอื่นเอาไปใช้ต่อได้ทันที
> (ค) หน้าหลักฐานความสำเร็จ 5 ด้าน ที่ยืนยันด้วยพฤติกรรม ไม่ใช่คะแนนที่นักเรียนตอบเอง

### 🟡 สำคัญแต่ไม่ผูกเดดไลน์

- [ ] **ผู้เชี่ยวชาญ 5 ท่านประเมิน IOC จริง** — ค่าในระบบยังเป็นข้อมูลจำลอง
      ข้อคำถามใหม่ 2 ข้อ (วินัย, ยอมรับความแตกต่าง) ยังไม่ผ่านการตรวจสอบเลย
- [ ] **เก็บข้อมูลจริงให้ครบ** — ปัจจุบัน 6/15 คน (ดูข้อ 5)
- [ ] **ให้ครูประเมินแบบสังเกต 9 ด้านกับนักเรียนจริงอย่างน้อย 3 คน**
      ระบบจึงจะคำนวณค่า r ในตารางที่ 6 ได้ (ตอนนี้ยังไม่มีข้อมูลจริงเลย)
- [ ] **ชวนนักเรียนกลับมารายงานผลการลงมือทำ** — หลักฐานด้านที่ 5 ยังว่างทั้งหมด
- [ ] **ทดลองใช้ภารกิจรายวันจริงหนึ่งสัปดาห์เต็ม (จ–ศ)** ระบบพร้อมแล้วแต่ยังไม่มีข้อมูลจริงสักแถว
- [ ] **ปรับรายงาน 5 บท** ให้ตรงกับระบบปัจจุบัน — รายงานยังเขียนไว้ว่า 7 ด้าน 10 ข้อ
      และยังไม่มี PWA, เช็คอินใจ, ระดับโรงเรียน, พระบรมราโชบาย 4 ด้าน, ภารกิจรายวัน, ระบบ 4 ชั้น
- [ ] **ภาพหน้าจอในภาคผนวก ค** เป็นเวอร์ชันเก่าก่อนปรับ UI ทั้งหมด (ก่อนมี navbar และหน้าแรกใหม่)
- [ ] **ตรวจบรรณานุกรม 28 รายการ** ยังไม่ได้เปิดต้นฉบับตรวจทีละรายการ
- [ ] **รัน `migratePulseRows()`** ใน Apps Script หนึ่งครั้ง

### 🟢 ทางเลือกพัฒนาต่อ

- [ ] เปลี่ยนรหัสครู `SaM2569` ที่เปิดเผยในโค้ดสาธารณะ
- [x] ~~แบบสังเกตพฤติกรรมโดยครู 9 ด้าน + สหสัมพันธ์~~ **ทำแล้ว 12 ส.ค.**
- [x] ~~ภารกิจรายวัน จ–ศ ที่รวมขึ้นเป็นสัปดาห์/เดือน/เทอม~~ **ทำแล้ว 12–13 ส.ค.**
- [x] ~~แถบเมนูบนสุดและหน้าแรกที่ครูทุกคนใช้ง่าย~~ **ทำแล้ว 13 ส.ค.**

---

## 3. สถาปัตยกรรมระบบ

```
index.html          ตัวโปรแกรมทั้งหมดในไฟล์เดียว (608 KB) ไม่มี build step ไม่มี dependency
manifest.webmanifest  PWA manifest ติดตั้งเป็นแอปได้
sw.js               Service Worker ใช้งานออฟไลน์ (VERSION ต้องเพิ่มทุกครั้งที่แก้ไฟล์แคช)
icons/              ไอคอน any + maskable
Code.gs             Apps Script backend เวอร์ชัน 2
```

**เก็บข้อมูล** localStorage ก่อนเสมอ แล้วซิงก์ขึ้น Google Sheet ของโรงเรียนเอง
ไม่มีเซิร์ฟเวอร์กลาง สอดคล้อง PDPA

**backend เก็บเป็น JSON ก้อนทึบ** (คอลัมน์ key/name/updated/json) โครงสร้างข้อมูลอยู่ฝั่งหน้าเว็บทั้งหมด
**เพิ่มฟีเจอร์ใหม่จึงไม่ต้องแก้ Apps Script เลย** — ทุกโมดูลที่เพิ่มมา 12–13 ส.ค. ไม่แตะ backend สักบรรทัด

**รูปแบบคีย์ข้อมูล**

| ประเภท | คีย์ |
|---|---|
| ภารกิจที่ 1 | `lc:<idx>` |
| ภารกิจที่ 2+ | `lc:m<mid>:<idx>` |
| ภารกิจด่วน | `lc:qdef:<code>` / `lc:q:<code>:<idx>` |
| ติดตามการลงมือทำ | `lc:act:<mid>:<idx>` |
| **เช็คอินใจรายวัน** | `lc:pulse:<YYYY-Www>:<idx>` — 1 แถว/คน/สัปดาห์ ภายในเก็บ `days{1..7}` |
| **ภารกิจรายวัน** | `lc:daily:<YYYY-Www>:<idx>` — 1 แถว/คน/สัปดาห์ เก็บครบทั้ง 5 วัน |
| **รอบภารกิจ 4 ชั้น** | `lc:cycle` — 1 แถว/ห้อง เก็บสถานะปิดรอบและด้านเป้าหมาย |
| ทะเบียนห้อง | `lc:school:rooms` |
| สรุประดับห้อง (ล่าสุด) | `lc:school:sum:<รหัสห้อง>` |
| **สรุประดับห้องรายสัปดาห์** | `lc:school:hist:<รหัสห้อง>:<YYYY-Www>` — ไม่เขียนทับ ใช้ทำแนวโน้ม |
| ตั้งค่า | `lc:cfg:room` `grade` `roster` `school` `roomcode` `teacher` |

**ลิงก์ส่งให้นักเรียน** `?m=<kind>&id=<id>` → `missionLink()` / `routeFromURL()`
kind ที่รองรับ `week` (ภารกิจประจำสัปดาห์) · `quick` (ภารกิจด่วน) · `daily` (id = `<สัปดาห์>:<วัน 1-5>`)
ทุกภารกิจและทุกวันมีลิงก์ของตัวเอง ครูกดคัดลอกส่งเข้าไลน์ห้องได้ทันที

---

## 4. กรอบการวัด

### สมรรถนะ 9 ด้าน · แบบวัด 10 ข้อ (7 ด้าน) · คะแนนเต็ม 40

**แบบวัดสถานการณ์ (SJT) มี 10 ข้อ ครอบคลุม 7 ด้าน** (`self` `think` `comm` `resp` `hon` `resp2` `common`)
ตรงกับที่รายงาน 5 บทระบุ และตรงกับฉบับที่ข้อมูลจริงของนักเรียนใช้เก็บ จึงเทียบก่อน–หลังได้โดยไม่ต้องมีหมายเหตุ

อีก 2 ด้าน (`disc` วินัยและการเคารพกติกา · `divers` การยอมรับความแตกต่าง) **ไม่ได้อยู่ในแบบวัด**
แต่เก็บหลักฐานผ่านทางที่เป็นพฤติกรรมจริงมากกว่า คือ **แบบสังเกตของครู (`TOBS` 9 ด้าน)** และ
**ภารกิจรายวัน (`DAILY_SIT` ครบ 9 ด้าน)** · ในโค้ดใช้ `ITEM_DIMS` สำหรับตารางที่มาจากแบบวัด
และใช้ `DIMS` สำหรับตารางที่มาจากการสังเกต **อย่าสลับกัน**

| กลุ่ม | ด้าน (key) |
|---|---|
| ทักษะชีวิต | การจัดการตนเอง `self` · การคิดและแก้ปัญหา `think` · การสื่อสารและทำงานร่วมกัน `comm` |
| พลเมืองดี | ความรับผิดชอบ `resp` · ความซื่อสัตย์ `hon` · การเคารพสิทธิผู้อื่น `resp2` · การคำนึงถึงประโยชน์ส่วนรวม `common` · วินัยและการเคารพกติกา `disc` · การยอมรับความแตกต่าง `divers` |

`disc` และ `divers` เพิ่มเมื่อ 11 ส.ค. 2569 เพื่อให้ครบตามกรอบพลเมืองดีของไทย
(คุณลักษณะอันพึงประสงค์ 8 ประการ · ค่านิยมหลัก 12 ประการ · จุดเน้นหน้าที่พลเมือง · กรอบ สกศ.)

### ชั้นสังเคราะห์ พระบรมราโชบาย (ค่าคงที่ `ROYAL` + `ROYAL_FIT`)

> **ถ้อยคำมาตรฐานที่ต้องใช้** (ค่าคงที่ `ROYAL_CLAIM`)
> *"สนับสนุนการดำเนินงานตามพระบรมราโชบายด้านการศึกษา โดยเน้นด้านคุณธรรมและการเป็นพลเมืองดี"*
> **ห้ามเขียนว่า "ครอบคลุมพระบรมราโชบายครบทั้ง 4 ด้าน"** เพราะเกินหลักฐานที่โปรแกรมเก็บได้จริง

| ด้าน | ระดับความสอดคล้อง | เหตุผล |
|---|---|---|
| พื้นฐานชีวิตที่มั่นคง มีคุณธรรม | **ตรงชัด** | มีข้อวัด ข้อฝึกรายวัน และแบบสังเกตรองรับโดยตรง |
| เป็นพลเมืองดี | **ตรงชัด** | มีข้อวัด ข้อฝึกรายวัน และแบบสังเกตรองรับโดยตรง |
| ทัศนคติที่ถูกต้องต่อบ้านเมือง | ตรงบางส่วน | แตะผ่านการเคารพสิทธิ/ยอมรับความต่าง/ส่วนรวมระดับห้องเรียน |
| มีงานทำ มีอาชีพ | ไม่ใช่จุดเน้น | โปรแกรมไม่ได้ออกแบบเพื่อด้านนี้ **ไม่ควรนำผลไปอ้าง** |


| ด้าน | สมรรถนะที่รองรับ |
|---|---|
| มีทัศนคติที่ถูกต้องต่อบ้านเมือง | `resp2` `divers` `common` |
| มีพื้นฐานชีวิตที่มั่นคง มีคุณธรรม | `hon` `disc` `think` |
| มีงานทำ มีอาชีพ | `self` `resp` `comm` |
| เป็นพลเมืองดี | `common` `resp` `disc` `resp2` |

### 4.3 แบบสังเกตพฤติกรรมของครู 9 ด้าน (`TOBS`)

ค่าคงที่ **`TOBS`** คือแบบสังเกตพฤติกรรมที่ครูเป็นผู้ให้ระดับ 1–4 ครบทั้ง 9 ด้าน
คีย์ตรงกับ `DIMS` ทุกตัว เก็บใน `record.teacher.score`

> **สำคัญมากเมื่อแก้โค้ด** ในไฟล์มีรูบริก **สองชุด อย่าสลับกัน**
> - `RUBRICS` 4 ด้าน = **นักเรียนประเมินตนเอง** (ขั้น Opt) คงไว้ 4 ด้านโดยตั้งใจ เพื่อไม่เพิ่มภาระนักเรียน
> - `TOBS` 9 ด้าน = **ครูสังเกตพฤติกรรม** ใช้คู่กับแบบวัดเพื่อหาค่า r

ความเข้ากันได้กับข้อมูลเดิม คีย์ `self` `think` `comm` เหมือนรูบริกเดิมทุกประการ
ระดับที่ครูเคยให้ไว้จึงใช้ต่อได้ทันที ส่วนคีย์ `civic` ของฉบับเดิมถูกแยกเป็น 6 ด้านย่อย
ระบบไม่เดาค่าให้ แต่แสดงเป็นแถว "ค่าจากแบบสังเกตฉบับเดิม" ในรายงานรายบุคคล
**ผลคือข้อมูลจริง 6 คนเดิมจะขึ้นเป็น 3/9 ด้าน ครูต้องให้ระดับอีก 6 ด้านเองก่อนจึงจะคำนวณ r ได้**

### 4.4 ความเที่ยงตรงเชิงสภาพ (Concurrent Validity)

ตารางที่ 6 ในหน้าสถิติ คำนวณสหสัมพันธ์แบบเพียร์สันระหว่าง
คะแนนรายด้านจากแบบวัดของนักเรียน (หลังเรียน) กับระดับที่ครูให้จากแบบสังเกต ทั้งคู่เป็นมาตร 1–4

| | วิธีนับ |
|---|---|
| รายด้าน | ตัดเป็นคู่ นับเฉพาะผู้เรียนที่มีทั้งสองค่าในด้านนั้น |
| ภาพรวม | นับเฉพาะผู้เรียนที่ครูประเมินครบ 9 ด้าน **และ** ทำแบบวัดครบทุกด้าน |

ทดสอบนัยสำคัญด้วย t = r√(df/(1−r²)) เมื่อ df = n−2
ค่า `r` ระดับห้องถูกส่งขึ้นแดชบอร์ดผู้บริหารเป็นคอลัมน์ "ความเที่ยงตรง r" ด้วย
(เป็นค่าสรุประดับห้องเท่านั้น ไม่มีคำตอบรายบุคคล ตาม PDPA เดิม)
จุดเทียบจากงานวิจัย ป.6 คือ r = 0.84

### 4.5 หลักฐานความสำเร็จ 5 ด้าน

ความสำเร็จของงานพลเมืองดีไม่ได้วัดจากคะแนนหลังเรียนที่สูงขึ้นอย่างเดียว
หน้า **หลักฐานความสำเร็จ 5 ด้าน** (`openEvid()`) รวบรวมหลักฐานครบทั้ง 5 ด้านจากข้อมูลจริงในระบบ

| ด้าน | แหล่งหลักฐาน | ตัวชี้วัด |
|---|---|---|
| 1 ความรู้และความเข้าใจหน้าที่/สิทธิ | แบบวัดสถานการณ์ 10 ข้อ ก่อน–หลัง + ขั้น U ของภารกิจรายวัน | % ถึงระดับสามารถ · t-test · % ตอบขั้น U ถูก |
| 2 คิดวิเคราะห์ตัดสินใจด้วยเหตุผล | ขั้น T และ O + บันทึกสนทนา AI โค้ช | ทางเลือก ≥ 2 พร้อมผู้เสียประโยชน์ · เหตุผลอ้างข้อมูล · การทบทวน · คะแนนด้าน `think` |
| 3 เคารพความแตกต่างและสิทธิผู้อื่น | แบบวัดรายด้าน + แบบสังเกตของครู | `resp2` `divers` + % ที่ครูเห็นระดับ 3 ขึ้นไป |
| 4 รับผิดชอบและจิตสาธารณะ | แบบสังเกตของครู + บทบาทในขั้น G | `resp` `common` + `disc` + % ระบุสิ่งที่ทำจริง |
| 5 ลงมือทำจริง | ขั้น W + โมดูลติดตามการลงมือทำ + ครูยืนยัน | % เสนอข้อตกลง · % กลับมารายงาน · % ทำจริง · % ครูยืนยันหลักฐาน |

**กฎการสรุปสถานะรายด้าน** มีตัวชี้วัดใดไม่ผ่าน = ยังไม่ถึงเกณฑ์ · มีตัวชี้วัดใดไม่มีข้อมูล = รอข้อมูล
ด้านจะ "ผ่านเกณฑ์" ได้เมื่อมีหลักฐานครบทุกตัวชี้วัดเท่านั้น
**จงใจไม่ให้ด้าน 3–4 ผ่านด้วยคะแนนที่นักเรียนตอบเองอย่างเดียว** ถ้าครูยังไม่ได้สังเกตพฤติกรรม
เพราะขัดกับหลักของกรอบนี้ที่ให้น้ำหนักหลักฐานพฤติกรรม

### 4.6 โมดูลติดตามการลงมือทำ (Action Follow-up)

เดิมขั้น W เก็บได้แค่ "คำสัญญา" ไม่มีอะไรตามว่าเกิดขึ้นจริงไหม โมดูลนี้ปิดวงจร

- **นักเรียน** หลังทำภารกิจเสร็จ การ์ดบนหน้าแรกจะชวนกลับมารายงานผล
  (ทำแล้ว / ทำได้บางส่วน / ยังไม่ได้ทำ + เล่าสิ่งที่เกิดขึ้น + ผลที่สังเกตได้)
  ถามซ้ำจนกว่าสถานะจะเป็น "ทำแล้ว" ถ้ายังไม่ได้ทำก็ถามว่าติดขัดอะไร
- **ครู** กดยืนยันหรือปฏิเสธหลักฐานได้จากตารางในหน้าหลักฐาน หรือกรอกแทนนักเรียน
- **คีย์ข้อมูล** `lc:act:<mid>:<idx>` ซิงก์ผ่านกลไกเดิม ไม่ต้องแก้ Apps Script

### 4.7 ภารกิจรายวัน จ–ศ และระบบต่อเนื่อง 4 ชั้น ⭐ หัวใจของระบบตอนนี้

**โครงเวลา** `ภารกิจรายวัน 5 วัน → รายสัปดาห์ 1 ครั้ง → รายเดือน 1 ครั้ง → รายเทอม 1 ครั้ง`

| วัน | บทบาทในวงจร |
|---|---|
| จันทร์ | รู้ + **ตั้งเป้าของสัปดาห์ด้วยตนเอง** (เลือกจากเมนู 2 ข้อ หรือพิมพ์เอง) |
| อังคาร | คิดวิเคราะห์ |
| พุธ | **ลงมือทำ** + บันทึกว่าใครได้ประโยชน์ (ทวนเป้าวันจันทร์ให้เห็น) |
| พฤหัส | เคารพผู้อื่น |
| ศุกร์ | สะท้อน + **ให้คะแนนตัวเอง 1–4** + **เล่าให้คนที่บ้านฟัง** |

**ทุกวันเดินครบกระบวนการ UP TO GROW ทั้ง 8 ขั้น ภายใน 10 นาที** (แก้ 13 ส.ค. — เดิมมีแค่ขั้น O ขั้นเดียว)

| ขั้น | คำถาม | วิธีตอบ | นาที | เก็บเป็น |
|---|---|---|---|---|
| **U** เข้าใจ | เรื่องนี้เป็นเรื่องเกี่ยวกับอะไรมากที่สุด | แตะ 1/4 | 1.5 | `u` `uOk` ถูก/ผิด |
| **P** เห็นปัญหา | ถ้าไม่มีใครทำอะไรเลย กระทบใครบ้าง | แตะ 1/4 | 1 | `sl.p` 1–4 |
| **T** คิดมีหลักฐาน | ก่อนตัดสินใจ ควรรู้อะไรเพิ่ม | แตะ 1/4 | 1 | `sl.t` 1–4 |
| **O** ตัดสินใจ | ถ้าเป็นเรา จะทำแบบไหน + เหตุผล | แตะ + พิมพ์ 1 ประโยค | 2.5 | **`level` 1–4** ← ข้อวัดผลจริง |
| **G** ทำงานกลุ่ม | ถ้าอยากให้เกิดผลจริง ควรทำร่วมกับใคร | แตะ 1/4 | 1 | `sl.g` 1–4 |
| **R** สะท้อนคิด | เคยเจอจริงไหม ตอนนั้นทำเหมือนที่เลือกไหม | แตะ 1/3 | 0.5 | `rSame` = same/diff/never |
| **O** ปรับปรุง | เจออีกครั้งจะทำให้ดีขึ้นตรงไหน | แตะ 1/4 | 1 | `sl.o2` 1–4 |
| **W** เพื่อส่วนรวม | เนื้อหาต่างตามวัน (ดูตารางบน) | แตะ (+พิมพ์ จ พ ศ) | 1.5 | `goalText` `act` `wTxt` `self` `told` |

**ทำไมออกแบบแบบนี้**
- **ขั้น O ตัดสินใจใช้ระดับ 1–4 จาก `DAILY_SIT` เหมือนเดิมทุกประการ** การรวมขึ้นสัปดาห์/เดือน/เทอม
  จึงไม่เปลี่ยนวิธีคิดเลย ทุกวันให้ระดับได้เหมือนกัน การรวมขึ้นชั้นบนจึงมีความหมายทางสถิติจริง
- อีก 7 ขั้นเป็นคำถาม **กระบวนการคิด** ไม่ใช่คำถามเนื้อเรื่อง จึงใช้กับทั้ง 30 สถานการณ์ในคลังได้
  โดยไม่ต้องเขียนคำถามใหม่ 30 ชุด และไม่ทำให้เครื่องมือวัดปนเปื้อน
- **ขั้น U วัดความรู้จริง มีถูก–ผิด** (ตัวเลือกคือกรอบความรู้ประจำสมรรถนะจาก `DFRAME` 9 ด้าน
  ข้อถูก 1 + ตัวลวง 3 สลับตำแหน่งแบบคงที่ทั้งห้อง) = **หลักฐานความสำเร็จด้านที่ 1**
  ที่เป็นความรู้ ไม่ใช่คำตอบที่นักเรียนประเมินตนเอง
- **ขั้น R เป็นกลไกตรวจสอบความตรง** คำตอบ "เคยเจอ แต่ตอนนั้นทำต่างจากที่เลือกวันนี้"
  คือช่องว่างระหว่างสิ่งที่รู้กับสิ่งที่ทำ รายงานสรุปให้ครูหยิบไปคุยในโฮมรูมได้
- ทุกขั้นแสดงว่ากำลังฝึก**คุณลักษณะพลเมืองดี**ด้านใด และตรงกับ**พระบรมราโชบาย**ด้านใด
  (วัตถุประสงค์โครงการทั้ง 2 ข้อ ปรากฏบนหน้าจอนักเรียนจริง ไม่ใช่แค่ในเอกสาร)
- แตะตอบเป็นหลัก พิมพ์เฉพาะขั้น O และขั้น W ของวันจันทร์ พุธ ศุกร์ จึงคุมเวลาได้ที่ 10 นาที
- ทำทีละขั้น (stepper) มีแถบความคืบหน้าและเวลาที่เหลือ · **ไม่บันทึกขึ้นชีตระหว่างทาง**
  บันทึกครั้งเดียวเมื่อครบ 8 ขั้น จึงไม่เกิดสถานะกลางคันค้างในชีต (บทเรียนจากบั๊ก 12 ส.ค.)

**คลังสถานการณ์ `DAILY_SIT`** 30 เรื่อง (5 วัน × 6 เรื่อง) หมุนตามเลขสัปดาห์
ยึด **มาตรฐานการออกแบบข้อเดียวกับแบบวัด SJT 10 ข้อ** (กฎ 5 ข้อเขียนไว้ในคอมเมนต์เหนือ `DAILY_SIT`)
สรุปคือ ตัวเลือกต้องเป็น**การกระทำที่สังเกตได้** ไม่ใช่ความรู้สึก · ไม่มีตัวเลือกที่ถูกอย่างโจ่งแจ้ง ·
ระดับ 1–4 ไล่จากหลีกเลี่ยง→ทำตามน้ำ→ทำถูกต้อง→ทำถูกต้องและชวนคนอื่นด้วย · ผูกกับ `DIMS` หนึ่งด้านเสมอ

**ตัวชี้วัดที่ระบบคำนวณ** `lvAvg` (ระดับเฉลี่ย) · `lvHi` (% ถึงระดับ 3+) · `byDay` · `byDim`
ใช้เหมือนกันทุกชั้น จึงเทียบข้ามสัปดาห์/เดือน/เทอมได้

**การปิดรอบ (`lc:cycle`)** **ครูเป็นคนกดเองว่านี่คือวันสุดท้าย / สัปดาห์สุดท้าย / เดือนสุดท้าย**
เมื่อปิดรอบ ระบบหา**ด้านที่อ่อนที่สุดจากคะแนนที่นักเรียนประเมินตนเองวันศุกร์**
แล้วตั้งเป็นด้านเป้าหมายของรอบถัดไปให้อัตโนมัติ (`CYCLE.focus[wk]` ซึ่ง `dailyDims()` อ่านก่อนการหมุนปกติ)
เปิดรอบใหม่ได้ ข้อมูลนักเรียนไม่หาย

**ฐานงานวิจัยที่ออกแบบจาก** ACT Evidence Review 2022 (dosage · วงจร active citizenship · ให้นักเรียนเลือกเอง) ·
Vercelloti & Matto 2016 (การเล่าให้ครอบครัวฟังทำให้ผลยั่งยืนกว่าอย่างชัดเจน) · สกศ. 2558 ·
รูปแบบตารางรายงานของโรงเรียนหนองยอง

> **ข้อฝึกรายวันเป็นคนละชุดกับแบบวัด 10 ข้อโดยเจตนา** ถ้าเอาข้อเดียวกันมาให้ทำซ้ำทุกวัน
> คะแนนหลังเรียนจะสูงขึ้นเพราะจำข้อได้ ไม่ใช่เพราะสมรรถนะเปลี่ยน

> ✅ **แก้แล้ว 13 ส.ค.** — ฉบับปัจจุบันกลับมาเป็น 10 ข้อ (เต็ม 40) เท่ากับข้อมูลจริงของนักเรียน 6 คน
> **คะแนนรวมจึงเทียบกันได้ตามปกติแล้ว** กลไกตรวจจับฉบับที่ต่างกันยังคงอยู่เผื่อกรณีอื่น
> (ข้อความเดิม: ข้อมูลเดิม 10 ข้อ ฉบับตอนนั้น 12 ข้อ)
> ระบบตรวจจับและเตือนอัตโนมัติในหน้าสถิติและหน้าครู
> คะแนนรวมเทียบกันไม่ได้ แต่คะแนนรายด้านและพระบรมราโชบายยังเทียบได้ เพราะเป็นค่าเฉลี่ย 1–4

---

## 5. ข้อมูลจริงที่มีอยู่

ภารกิจที่ 1 เงินส่วนรวมของห้อง · ม.4/4 · 15 คน · เก็บเมื่อ 6–7 ส.ค. 2569

| สถานะ | จำนวน |
|---|---|
| ทำครบทั้งก่อนและหลัง | **6 คน** |
| ทำแบบวัดก่อนแล้ว แต่ค้างกลางภารกิจ | 9 คน |
| ภารกิจรายวัน · แบบสังเกตของครู 9 ด้าน · การติดตามการลงมือทำ | **ยังไม่มีข้อมูลจริงเลย** |

**สถิติเบื้องต้น (n=6)** t = 0.200, df = 5, p = .849 → ยังไม่มีนัยสำคัญ (n น้อยเกินไป)

**ข้อค้นพบสำคัญที่มีค่ากว่าคะแนน** กลไกตรวจสอบความตรงจับได้ 2 กรณีจริง

1. นักเรียนทำแบบวัดก่อนเรียนได้เต็ม 40/40 แต่ตอบเองว่า
   *"เคยเจอ แต่ตอนนั้นทำต่างจากที่เลือกไว้ตอนนี้"*
2. นักเรียนได้เต็ม 40/40 ทั้งก่อนและหลัง แต่ตอบว่า
   *"จะยอมตามเสียงส่วนใหญ่เพราะไม่อยากมีปัญหา"*

เป็นหลักฐานเชิงประจักษ์ว่ากลไก 3 ชั้นทำงานจริง ตรงกับงานวิจัย social desirability bias
**ใช้เล่าในเวที Symposium ได้ดีกว่าตัวเลขคะแนน**

**เหตุผลที่ 9 คนทำไม่จบ** ติดขั้น O ตัดสินใจมากที่สุด (5 คน) เพราะต้องพิมพ์เยอะ
แก้แล้วโดยลดช่องบังคับจาก 26 เหลือ 16 ช่อง

---

## 6. ความสามารถของระบบ

**หน้าแรกและการนำทาง** แถบเมนูบนสุดแบบ navbar — `ภารกิจ` (4 ระดับ) · `สำหรับครู` · `ผู้บริหาร` · `⋯`
หน้าแรกเหลือเพียง 4 ปุ่มของนักเรียน + แถบ 4 ระดับของครู · ปุ่มแตะ ≥44px ทุกจุด

**ฝั่งนักเรียน** ภารกิจ 8 ขั้น UP TO GROW (ไม่เกิน 20 นาทีรวมแบบวัด) · **ภารกิจรายวัน จ–ศ** · แบบวัดก่อน–หลัง 10 ข้อ ·
AI Life Coach (ตั้งคำถาม ไม่ให้คำตอบ) · เช็คอินใจ 60 วินาที/สัปดาห์ · ระบบจำจุดที่ค้างไว้

**ฝั่งครู** Dashboard เรียลไทม์ · **หน้าปฏิทินภารกิจรายวัน จ–ศ พร้อมวันที่จริงและปุ่มคัดลอกลิงก์รายวัน** ·
**หน้าสรุปแยก 4 ระดับพร้อมปุ่มปิดรอบ** · หน้าติดตามรายวัน (ตาราง 15×5 + บันทึกแทนกระดาษ) ·
รายงานรายสัปดาห์/เดือน/ภาคเรียน · วิเคราะห์สถิติอัตโนมัติ (t-test, E.I., Cohen's d, รายด้าน,
พระบรมราโชบาย, สหสัมพันธ์ความเที่ยงตรงเชิงสภาพ) · แบบสังเกตพฤติกรรม 9 ด้าน · คำนวณ IOC ·
กลไกตรวจสอบความตรง 3 ชั้น · สัมภาษณ์เชิงกระบวนการคิด · แดชบอร์ดสุขภาวะ ·
สร้างแผนการสอนและใบงานอัตโนมัติ · โหมดกรอกแทนสำหรับเด็กไม่มีมือถือ · ส่งออก CSV

**ฝั่งผู้บริหาร** แดชบอร์ดระดับโรงเรียน เทียบทุกห้อง · ผลพระบรมราโชบายถ่วงน้ำหนัก · ห้องที่ควรสนับสนุนก่อน
**เห็นเฉพาะค่าสรุประดับห้อง ไม่มีคำตอบรายบุคคล**

**การเข้าถึง** PWA ติดตั้งได้ทั้ง Android/iOS ไม่ผ่าน App Store · ออฟไลน์เต็มรูปแบบ ·
ปรับขนาดตัวอักษร 85–160% · โหมดคมชัดสูง · ใบงานกระดาษ · ใช้ได้ทุกระดับชั้น

---

## 7. ข้อควรรู้ทางเทคนิค (สำคัญมากเมื่อแก้ไขต่อ)

1. **ไฟล์งานจริงชื่อ `index (30).html`** ในโฟลเดอร์ `โปรแกรม up to grow`
   ตอน push ต้องคัดลอกเป็น `index.html`
2. **ตรวจ syntax ทุกครั้ง** ดึง `<script>` ออกมาแล้ว `node --check`
   และตรวจ inline handler ทุกตัวด้วย `new Function()` เพราะ handler ที่สร้างจาก template
   จะพังตอนคลิกเท่านั้น ไม่พังตอนโหลด
3. **`read_console_messages` เก็บ error ค้างข้ามการรีโหลด** ให้ยืนยันด้วยการเช็กว่า
   สัญลักษณ์ท้ายสคริปต์ (`checkResume`, `scrollToMissions`) ถูกนิยามหรือยัง
4. **ทดสอบหน้าที่เรียกเครือข่าย** ต้อง stub `window.pullAll` และ `window.apiCall` ก่อน
   ไม่งั้น `openTeacher()` จะค้าง
5. **แก้ `sw.js` ต้องเพิ่ม `VERSION`** ตอนนี้อยู่ที่ **`v17`** ครั้งถัดไปเป็น `v18`
   ไม่งั้นเครื่องที่ติดตั้งแอปแล้วจะไม่ได้ไฟล์ใหม่
6. **`seedDemo()` ไม่ทับข้อมูลจริง** ตรวจ `demo===true` ก่อนเขียนทุกครั้ง (แก้บั๊กนี้แล้ว)
7. **Apps Script cold start ~10–17 วินาที** ครั้งถัดไป 2–3 วินาที
   แนะนำครูเปิดหน้าครูทิ้งไว้ก่อนเข้าคาบเพื่อปลุกเซิร์ฟเวอร์
8. **ยังต้องรัน `migratePulseRows()`** ใน Apps Script หนึ่งครั้ง
   เพื่อย้ายข้อมูลเช็คอินใจเดิมจากชีต `responses` ไปชีต `pulse`
9. **มีรูบริก 2 ชุด อย่าสลับกัน** `RUBRICS` = นักเรียนประเมินตนเอง 4 ด้าน ·
   `TOBS` = ครูสังเกตพฤติกรรม 9 ด้าน (ดูข้อ 4.3) เวลาแก้ให้ดูก่อนว่าจุดนั้นเป็นของนักเรียนหรือของครู
10. **ตรวจสูตรสถิติใหม่ทุกครั้งด้วย `scipy.stats`** `pairedT` และ `pearson` ตรวจแล้วตรงถึงหลักที่ 15
    (`stats.ttest_rel` และ `stats.pearsonr`) ถ้าแก้สูตรต้องรันเทียบซ้ำ
11. **สถานะของนักเรียนต้องสอดคล้องกันเสมอ** `phase` ต้องตรงกับ `step` **ก่อน** เรียก `save()`
    เพราะ `save()` ส่งขึ้นชีตทันที ถ้าบันทึกสถานะกลางคัน ชีตจะเก็บค่าที่เป็นไปไม่ได้ค้างถาวร
    และอย่าเข้าถึง `STEPS[d.step]` ตรง ๆ ให้ใช้ `progressText(d)` ที่กันค่านอกขอบเขตไว้แล้ว
12. **หน้าที่ขึ้นข้อความ "กำลังโหลด…" ต้องมีตัวดักข้อผิดพลาด** ไม่งั้นข้อผิดพลาดตอนวาดหน้า
    จะกลายเป็นข้อความค้างที่ทำให้เข้าใจผิดว่าต่อเครือข่ายไม่ได้ (`openTeacher` ทำแล้ว)
13. **อ่านข้อมูลแบบกลุ่มต้อง `pullAll(prefix)` ครั้งเดียวแล้วอ่านจากเครื่อง** ห้ามใช้
    `S.get(key, true)` วนหลายคีย์ เพราะคีย์ที่ไม่มีข้อมูลจะยิงเครือข่ายทีละคีย์จนหน้าค้าง
    (`dailyRec` จึงรับพารามิเตอร์ `remote` และเปิดใช้เฉพาะตอนนักเรียนเปิดรายคน)
14. **ฟังก์ชันที่แก้ค่าแล้ววาดหน้าใหม่ ต้องเก็บค่าจากช่องกรอกก่อนเสมอ** ไม่งั้นข้อความที่ผู้ใช้
    พิมพ์ค้างไว้จะหายไปเงียบ ๆ (บั๊กวันศุกร์ · แก้ด้วย `dGrab()` ที่เก็บทุกช่องก่อน re-render)
15. **ออกแบบข้อภารกิจใหม่ ให้ยึดกฎ 5 ข้อในคอมเมนต์เหนือ `DAILY_SIT`** อย่าเขียนตามใจ
    ไม่งั้นข้อใหม่จะให้ระดับที่เทียบกับข้ออื่นไม่ได้ แล้วการรวมขึ้นชั้นบนจะเพี้ยนทั้งระบบ

---

## 8. ประวัติการพัฒนา (ล่าสุดอยู่บนสุด)

| วันที่ | สิ่งที่ทำ |
|---|---|
| 13 ส.ค. | **Teacher Demo View — สาธิตว่า "ครูเห็นอะไร"** — มุมมองใหม่ `v-tdemo` ในแอปครู จำลองห้อง 40 คน (`TDEMO_ROOM` = ม.2/5 ห้องสาธิต) ขณะกำลังทำกิจกรรมกลางคาบ: 33 คนส่งครบ · 5 คนกำลังทำอยู่คนละขั้น · 2 คนยังไม่เริ่ม · ใช้**ชื่อสมมติ** (ชื่อจริง + อักษรย่อ) ไม่ใช่รายชื่อนักเรียนจริง · **สร้างคำตอบรายข้อ q0..q9 จริง** แล้วให้ `total()`/`dimScore()`/`pairedT()`/`effIndex()`/`pearson()` คำนวณตามปกติ ตัวเลขทุกบรรทัดจึงผ่านสูตรเดียวกับข้อมูลจริง · 7 ส่วนที่ครูเห็น: (1) ระหว่างคาบใครอยู่ขั้นไหน — การ์ด 40 ใบแยกสีเขียว/เหลือง/เทา (2) **ระบบคัดกรองว่าควรคุยกับใครวันนี้** รวมสัญญาณขอความช่วยเหลือ + อารมณ์ต่ำติดกัน + ยังไม่เริ่ม + ยังไม่เช็คอิน (3) ผลก่อน–หลังทั้งห้อง + กราฟแท่งรายด้าน + โดนัทระดับที่ครูให้ + เส้นรายบุคคล (4) เช็คอินใจ จ–ศ (5) ตารางคะแนนรายบุคคล 40 แถว (6) สถิติ t-test/E.I./ความเที่ยงตรง r (7) ครูต้องลงแรงเท่าไร · **ความปลอดภัยชุดเดียวกับ v16**: หน่วยความจำล้วน ไม่เขียน localStorage ไม่เรียก `queueRemote`/`flushRemote` ไม่แตะ `ROSTER`/`curMission` (มีเทสยืนยันทุกข้อ) · เชื่อมสองมุมมองสาธิตครู↔ผู้บริหารด้วยปุ่มข้ามไปมา · เทส `tdemo.py` 37/37 + regression เดิมผ่านครบ · `sw.js` = `v17` |
| 13 ส.ค. | **School Demo View — สาธิตการขยายผลระดับโรงเรียน 79 ห้อง** (สำหรับ Symposium 14 ส.ค.) — มุมมองใหม่ `v-sdemo` ในแอปผู้บริหาร จำลองโรงเรียนมัธยมขนาดใหญ่ ม.1–ม.3 ระดับละ 14 ห้อง, ม.4 = 13, ม.5–ม.6 ระดับละ 12 (รวม 79 ห้อง ~3,000 คน) · **ข้อมูลเป็น room summary ในหน่วยความจำเท่านั้น** สร้างด้วย seed คงที่ (`sdemoData()` + `SDEMO_CACHE`) เปิดกี่ครั้งได้เลขเดิม · **ไม่เรียก `LS.set`/`queueRemote`/`flushRemote` แม้แต่บรรทัดเดียว** จึงไม่ปนกับข้อมูลจริงและไม่ขึ้น Google Sheet (มีเทสยืนยัน: localStorage ไม่เปลี่ยนแม้แต่คีย์เดียว) · แสดง: ภาพรวมโรงเรียน / การ์ด 6 ระดับชั้นกดเจาะได้ / heatmap ครบ 79 ห้อง + สรุปย่อรายระดับ / ห้องที่ควรสนับสนุนก่อน (กติกาเดียวกับของจริง ~19 ห้อง) / แนวโน้ม 8 สัปดาห์เล่าเรื่องนำร่อง 12 ห้อง→ครบ 79 / วัตถุประสงค์ 2 ข้อ / ตารางพระบรมราโชบายพร้อม `ROYAL_FIT` / การ์ด "โรงเรียนอื่นต้องเตรียมอะไร" · แถบเตือน **"ข้อมูลจำลอง…ไม่ใช่ผลการดำเนินงานจริง"** ติดจอ+ติดงานพิมพ์+หัว CSV · เข้าถึง: เมนูผู้บริหาร หรือปุ่ม 🧪 ในแดชบอร์ดจริง มีปุ่ม "กลับสู่ข้อมูลจริง" · วิธีสร้างตัวเลข: ตั้งเป้า E.I. รายห้องก่อน (`eiT`) แล้วถอยกลับเป็นพัฒนาการรายด้าน กำหนดจุดอ่อนทั้งโรงเรียนที่ การคิดแก้ปัญหา/ประโยชน์ส่วนรวม ให้ heatmap ชี้เป้าได้จริง · เทส `sdemo.py` 35/35 + regression เดิมผ่านครบทุกชุด · `sw.js` = `v17` |
| 13 ส.ค. | **ภารกิจด่วนเลือกเป้าหมายการเรียนรู้ได้ + AI ต้องสร้างให้ตรง** — เพิ่ม `QGOALS` 8 เป้าหมาย เลือกได้หลายข้อ + พิมพ์เพิ่มเอง (`qGoalTxt`) · ส่งเข้า prompt โดยตรงพร้อมกฎว่า **ห้ามเปลี่ยนไปเรื่องอื่น** และ **ต้องวัดเป้าหมายให้ครบทุกข้อ** · AI ต้องคืน `goalsCovered` กลับมา ระบบจับคู่รายข้อด้วย `qGoalHit()` แล้วขึ้นป้ายสีเขียว/เหลืองต่อเป้าหมาย · `qOnTopic()` ตรวจว่าสถานการณ์พูดถึงหัวข้อจริง (เลื่อนหน้าต่าง 6 ตัวอักษร เพราะไทยไม่เว้นวรรค) ถ้าไม่ผ่าน **เตือนครู ไม่ปฏิเสธทิ้ง** · **แก้บั๊ก: เกณฑ์สำรองเดิมใช้ชุดของ `think` กับทุกสมรรถนะ** เลือกพลเมืองดีก็ได้เกณฑ์การคิด แก้เป็น `QRUBRIC` แยก 5 ชุด · **แก้ Code.gs: `max_tokens` 300 เล็กเกินจน JSON ถูกตัดกลางคัน** ทำให้ตกไปใช้โครงร่างสำรองทุกครั้งแม้ใส่ API key แล้ว เปลี่ยนเป็น 2000 และอัปเดต model id |
| 13 ส.ค. | **แยกเป็น 3 แอปตามบทบาท + ปิดช่องว่างฝั่งนักเรียนและผู้บริหาร** — (1) แถบเมนูเป็น 🎒 นักเรียน · 🧑‍🏫 ครู · 🏫 ผู้บริหาร · ภารกิจ 4 ระดับย้ายไปเมนูครู (2) **`applyRoleUI()` ทำให้หน้าแรกแสดงตามบทบาทจริง** นักเรียนไม่เห็นบล็อกครูและบล็อกอธิบายนวัตกรรม หน้าแรกสั้นลงจาก 3224px เหลือ 1197px ปุ่มภารกิจวันนี้ขึ้นมาอยู่ในจอแรก (3) **หน้า `growth` พัฒนาการของฉัน** รวมภารกิจรายวัน แบบวัดก่อน-หลัง แบบสังเกตของครู เช็คอินใจ และสิ่งที่ลงมือทำจริง · **แยกที่มาของค่าเป็น 3 แหล่ง ไม่รวมเป็นคะแนนเดียว** (4) **Civic Competency Heatmap** ห้อง × สมรรถนะ ใช้แถบระดับ 5 ขั้นพร้อมคำอธิบายสี (`HEAT_BANDS`) เพราะไล่สีต่อเนื่องแยกไม่ออกในช่วง 2.5-4.0 (5) **`lc:school:hist:<ห้อง>:<สัปดาห์>`** เก็บสรุปห้องรายสัปดาห์แทนการเขียนทับ เปิดทางให้แนวโน้มระดับโรงเรียน 8 สัปดาห์ (6) การ์ด **ผลตามวัตถุประสงค์ 2 ข้อ** บนหน้าผู้บริหาร |
| 13 ส.ค. | **ปรับข้ออ้างให้ไม่เกินหลักฐาน + ทำให้ตรงกับรายงาน 5 บท** — (1) **แบบวัดกลับเป็น 10 ข้อ เต็ม 40** ตรงกับรายงานและตรงกับข้อมูลจริงของนักเรียน 6 คน คะแนนรวมเทียบกันได้แล้ว · เพิ่ม `ITEM_DIMS` แยกด้านที่มาจากแบบวัด (7) ออกจากด้านทั้งระบบ (9) ทุกตาราง (2) **เวลา 8 ขั้น = 14 นาที + แบบวัดก่อน/หลังอย่างละ 3 นาที = ไม่เกิน 20 นาที** (`STEPMIN` `TESTMIN` `TOTALMIN`) แก้ทั้งแผนภาพระบบและแผนการสอน (3) **พระบรมราโชบาย** เพิ่ม `ROYAL_FIT` ระบุระดับความสอดคล้องรายด้าน (ตรงชัด/ตรงบางส่วน/ไม่ใช่จุดเน้น) และ `ROYAL_CLAIM` เป็นถ้อยคำมาตรฐาน · เลิกใช้คำว่า "ครอบคลุมครบทั้ง 4 ด้าน" ทุกจุด (4) **แยกบทบาท** เพิ่มตาราง "ใครทำอะไรในนวัตกรรมนี้" (`MODEL_ROLES`) · Outcome เปลี่ยนจาก "ทักษะชีวิตและพลเมืองดีสูงขึ้น" เป็น "ครูมีหลักฐานเชิงประจักษ์สำหรับตัดสินพัฒนาการ" (5) **แผนภาพ Input-Process-Output-Outcome** อัปเดตให้มีเช็คอินใจรายวัน ภารกิจรายวัน และข้อมูลสุขภาวะ |
| 13 ส.ค. | **เช็คอินใจเปลี่ยนจากรายสัปดาห์เป็นรายวัน** — คีย์เดิม `lc:pulse:<สัปดาห์>:<เลขที่>` แต่ภายในเก็บ `days{1..7}` แบบเดียวกับภารกิจรายวัน (1 แถว/คน/สัปดาห์เท่าเดิม **ไม่แตะ backend**) · ฟอร์มรายวัน ~30 วินาที = อารมณ์ 1 ข้อ + คำถามของวัน 1 ข้อ + ปุ่มขอความช่วยเหลือ (คำถาม 5 ข้อของสัปดาห์ถูกแจกวันละข้อ ครบสัปดาห์จึงครอบคลุมมิติเท่าเดิม) · ประตู `needPulse` เช็ค **ของวันนี้** · `pulseEntries`/`pulseOf` รองรับข้อมูลเดิมแบบสัปดาห์อัตโนมัติ · แดชบอร์ดครูใหม่: ภาพวันนี้ · ตาราง จ–ศ (เช็คอิน/อารมณ์เฉลี่ย/อารมณ์ต่ำ) · ตารางรายบุคคล 5 ช่องวัน · สัญญาณใหม่ "อารมณ์ต่ำติดกัน N วัน" · CSV รายวัน · ครอบด้วยตัวดักข้อผิดพลาด · สรุประดับโรงเรียนส่ง `avg` (วันนี้) `wAvg` `days` `people` `low` `help` `rate` ผู้บริหารเห็นคอลัมน์ สุขภาวะวันนี้ + เช็คอินสัปดาห์นี้ % · การ์ดหน้าแรกบอก "วันนี้ทำแล้วกี่คน" ทั้งภารกิจรายวันและเช็คอินใจ (`paintHomeToday`) |
| 13 ส.ค. | **โครงสร้างการเข้าใช้งานใหม่ 3 ข้อ** — (1) หน้าแรกเป็น**หน้าเลือกบทบาท** นักเรียนเข้าได้ทันที ครู/ผู้บริหารใส่รหัสเดียวกัน (`TEACHER_PIN`) ครูไปหน้าแรกโหมดครู ผู้บริหารไปแดชบอร์ดโรงเรียน จำบทบาทใน `sessionStorage` (`lc:role`) ปิดแท็บแล้วถามใหม่ (2) **ลิงก์ภารกิจที่ครูส่งข้ามหน้าเลือกบทบาท** เข้าภารกิจทันทีและตั้งบทบาทเป็นนักเรียนให้เอง (3) **เช็คอินใจก่อนเข้าทุกภารกิจ** ผ่านประตู `needPulse` ครอบ `startStudent`/`qOpen`/`dailyOpen` — ยังไม่เช็คอินสัปดาห์นี้ระบบพาไปเช็คอินก่อนแล้วเด้งกลับเข้าภารกิจต่อทันที เช็คอินแล้วผ่านเลยไม่ถามซ้ำ (เช็คอินเป็นข้อมูลรายสัปดาห์ 1 แถว/คน) · view ใหม่ `role` · `lockTeacher` กลับไปหน้าเลือกบทบาท |
| 13 ส.ค. | **ภารกิจรายวันเดินครบกระบวนการ UP TO GROW ทั้ง 8 ขั้น ภายใน 10 นาที** — เดิมมีแค่ขั้น O ตัดสินใจขั้นเดียวจบ · ทำเป็น stepper 8 หน้า มีแถบความคืบหน้าและเวลาที่เหลือ (`dailyBar` · `DMIN` รวม 10 นาที) · ขั้น U วัดความรู้จริงมีถูก-ผิดจาก `DFRAME` 9 กรอบ = หลักฐานด้านที่ 1 · ขั้น P T G O ปรับปรุง เป็นคำถามกระบวนการคิดใช้ได้กับทั้ง 30 สถานการณ์ (`dStepDef`) · ขั้น R เป็นกลไกตรวจสอบความตรงรายวัน (`rSame`) · ขั้น W ผูกมัดให้ลงมือทำจริงทุกวันรวมวันอังคารและพฤหัสบดี (`dailyWStep`) · **ขั้น O ตัดสินใจยังใช้ `level` 1-4 เหมือนเดิม การรวมขึ้นสัปดาห์/เดือน/เทอมจึงไม่เปลี่ยน** · บันทึกครั้งเดียวเมื่อครบ 8 ขั้น ไม่เกิดสถานะกลางคันในชีต · เพิ่มตัวชี้วัดใหม่ในรายงาน 3 แถว + ตารางกลไกตรวจสอบความตรง · ทุกหน้าบอกว่ากำลังฝึกคุณลักษณะพลเมืองดีด้านใดและตรงกับพระบรมราโชบายด้านใด |
| 13 ส.ค. | **ภารกิจรายวันเป็นสถานการณ์ทั้ง 5 วัน + ลิงก์ทุกภารกิจ** — คลัง `DAILY_SIT` 30 สถานการณ์ (6 ข้อ × 5 วัน) ยึดมาตรฐาน SJT เดียวกับแบบวัด 12 ข้อ: ตัวเลือก 4 ข้อเป็นการกระทำที่สังเกตได้ ระดับ 1-4 ผูกกับ `DIMS` · **ทุกวันจึงให้คะแนนระดับได้ ทำให้รวมขึ้นเป็นภารกิจรายสัปดาห์ได้จริง** (เดิมมีแค่วันอังคารที่ให้ระดับ) · ตัวชี้วัดเปลี่ยนจาก `monOk/tueHi` เป็น `lvAvg/lvHi/byDay/byDim` ทุกจุด · เพิ่ม `kind=daily` ใน `missionLink`/`routeFromURL` (`id = <week>:<วัน 1-5>`) + `DAILY_FORCE` ให้ลิงก์เจาะจงวันได้ · **เอาการ์ดเลือกภารกิจรายสัปดาห์ออกจากหน้าแรก** ย้ายไป `openWeekMissions()` และระดับ 2 · แก้บั๊กวันศุกร์ที่การแตะให้คะแนนตัวเองล้างข้อความที่พิมพ์ไว้ (`dGrab()`) |
| 13 ส.ค. | **แถบเมนูบนสุด (navbar) + หน้าแรกใหม่** — เมนู `ภารกิจ` (4 ระดับ) · `สำหรับครู` · `ผู้บริหาร` · `⋯` (ขนาดตัวอักษร/คมชัดสูง/ส่งข้อมูลซ้ำ) ย้ายขึ้นบนทั้งหมด · หน้าแรกเหลือ 4 ปุ่มนักเรียน + แถบ 4 ระดับของครู · เพิ่ม `openDayPlan()` แสดงภารกิจรายวัน **ตามปฏิทินจริง จ–ศ พร้อมวันที่** และ `openLevel(period)` หน้าสรุปแยกแต่ละระดับ พร้อมปุ่มปิดรอบในหน้าเดียวกัน · ปุ่มแตะ ≥44px ทุกจุด |
| 12 ส.ค. | **เพิ่มระบบภารกิจต่อเนื่อง 4 ชั้น** (`openCycle` · คีย์ `lc:cycle`) — วัน→สัปดาห์→เดือน→เทอม โดย**ครูกดปิดรอบเอง** (วันสุดท้าย/สัปดาห์สุดท้าย/เดือนสุดท้าย) · ปิดรอบแล้วระบบหา**ด้านที่อ่อนที่สุดจากคะแนนประเมินตนเองวันศุกร์** แล้วตั้งเป็นเป้าของรอบถัดไปอัตโนมัติ (`CYCLE.focus[wk]` ซึ่ง `dailyDims()` อ่านก่อนการหมุนเวียนปกติ) · เปิดรอบใหม่ได้ ข้อมูลนักเรียนไม่หาย · ตารางสรุปเทอมพิมพ์แนบรายงานได้ |
| 12 ส.ค. | **เพิ่มภารกิจรายวัน จ–ศ** (`lc:daily:<week>:<idx>` 1 แถว/คน/สัปดาห์ ไม่แก้ backend) — จ รู้ · อ คิด · พ ลงมือ · พฤ เคารพผู้อื่น · ศ สะท้อน+เล่าที่บ้าน ครบวงจร ACT · ด้านเป้าหมายหมุนครบ 9 ด้าน/5 สัปดาห์ · หน้าครูติดตาม (ตาราง 15×5 + บันทึกแทนกระดาษ) · รายงานรายสัปดาห์/เดือน/ภาคเรียน ตารางแบบองค์ประกอบ ๔ ของรายงานโครงการฯ + แผนที่พระบรมราโชบาย · เชื่อมเข้าหลักฐาน 5 ด้าน · ฐานวิจัย: ACT 2022 (dosage, วงจร, เลือกเอง), Vercelloti & Matto 2016 (เล่าที่บ้าน), สกศ. 2558 · **ข้อฝึกรายวันเป็นคนละชุดกับแบบวัด 12 ข้อโดยเจตนา** |
| 12 ส.ค. | **แก้บั๊กหน้าครูค้างที่ "กำลังดึงข้อมูลจาก Google Sheet…"** — ไม่ใช่ปัญหาเครือข่าย แต่ `STEPS[d.step]` อ่านค่านอกขอบเขตจากข้อมูลจริงที่ `step=8` คู่กับ `phase='mission'` · แก้ต้นเหตุที่ `nextStep()` · เพิ่ม `progressText()` · กู้สถานะอัตโนมัติใน `route()` · ครอบ `openTeacher` ด้วยตัวดักข้อผิดพลาด |
| 12 ส.ค. | เพิ่มหน้าหลักฐานความสำเร็จ 5 ด้าน (`openEvid`) · โมดูลติดตามการลงมือทำ (`openAct` คีย์ `lc:act:`) · การ์ดชวนรายงานผลบนหน้าแรก · แก้บั๊ก `checkResume` ที่ไม่ล้างกล่องเดิม ทำให้การ์ดค้าง |
| 12 ส.ค. | เพิ่ม `TOBS` แบบสังเกตพฤติกรรมของครู 9 ด้าน · ตารางที่ 6 ความเที่ยงตรงเชิงสภาพ (Pearson r ตรวจกับ scipy แล้ว) · คอลัมน์ r ในแดชบอร์ดผู้บริหาร · แบบบันทึกการสังเกตแบบกระดาษ · แก้บั๊ก pill โหมดไม่มีมือถือที่ยังค้างเลข 10 |
| 11 ส.ค. | ปรับกรอบเป็น 9 ด้าน 12 ข้อ · เพิ่มพระบรมราโชบาย 4 ด้าน · เพิ่มแดชบอร์ดผู้บริหารระดับโรงเรียน |
| 11 ส.ค. | ปรับ backend Apps Script เวอร์ชัน 2 (ค้นหาด้วยคอลัมน์ key · แยกชีต pulse · saveMany · prefix filter) |
| 10 ส.ค. | เพิ่มโมดูลเช็คอินใจ · Pulse (ตรวจสุขภาวะรายสัปดาห์) |
| 10 ส.ค. | ทำเป็น PWA ติดตั้งเป็นแอปได้ · ออฟไลน์เต็มรูปแบบ |
| 10 ส.ค. | ลดช่องบังคับกรอก 26 → 16 · ขยายตัวอักษร · แก้บั๊กคอนทราสต์ |
| 7–9 ส.ค. | รายงาน 5 บท · ภาพหน้าจอ · สถิติ · IOC · แผนการสอน · โหมดไม่มีมือถือ |

---

## 9. ไฟล์สำคัญในเครื่อง

โฟลเดอร์ `C:\Users\KruSam\Downloads\โปรแกรม up to grow\`

| ไฟล์ | คำอธิบาย |
|---|---|
| `index (30).html` | **ไฟล์งานจริง** แก้ที่นี่ (ตรงกับ `index.html` ใน repo ทุกไบต์) |
| `Code_ใหม่.gs` | Apps Script เวอร์ชัน 2 (deploy แล้ว) |
| `pwa/` | manifest, sw.js (`v17`), icons, README |
| `PROJECT_STATUS.md` · `NEXT_STEPS.md` | เอกสารสองฉบับนี้ (อยู่ใน repo ด้วย) |
| `รายงานนวัตกรรม_UPTOGROW_ไฮไลท์ส่วนที่เพิ่ม.docx` | รายงาน 5 บท 46 หน้า (ส่วนที่เพิ่มไฮไลท์เหลือง) |
| `ภาพหน้าจอโปรแกรม/` | ภาพหน้าจอ 16 ภาพ (เวอร์ชันเก่า ก่อนมี navbar) |
| `.claude/settings.json` | เปิด `bypassPermissions` ไว้ (อยู่ในเครื่องเท่านั้น ไม่ขึ้น repo) |

**เอกสารวิจัยอ้างอิงในโฟลเดอร์** ACT 2022 ทั้ง 3 ฉบับ (Teacher / Senior Leaders / Journeys) ·
`1446-file.pdf` (สกศ. 2558) · `รายงาน.pdf` (โรงเรียนหนองยอง) ·
`4.-พระบมราโชบายด้านการศึกษา.pdf` · `การสร้างแบบวัดความเป็นพลเมืองดี.pdf` · และอื่น ๆ รวม 16 ไฟล์
