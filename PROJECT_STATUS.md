# สถานะโปรเจกต์ Life Competency UP TO GROW

> ไฟล์นี้สรุปสถานะทั้งหมดของโปรเจกต์ ณ **11 สิงหาคม 2569**
> ใช้เป็นจุดตั้งต้นเมื่อเปิดการสนทนาใหม่ อ่านไฟล์นี้ก่อนเริ่มงานต่อ
> ไฟล์สั้นสำหรับเริ่มงานเร็ว ดูที่ [NEXT_STEPS.md](NEXT_STEPS.md)

---

## 1. ภาพรวม

**ผู้พัฒนา** นายศราวุธ พิมศร · ครู วิทยฐานะชำนาญการ
กลุ่มสาระการเรียนรู้คณิตศาสตร์ โรงเรียนมัธยมวัดหนองแขม · สพม.กทม. เขต 1

**นวัตกรรม** ระบบจัดกิจกรรมโฮมรูมเชิงสถานการณ์ พัฒนาทักษะชีวิตและความเป็นพลเมืองดี
พร้อมระบบวัดผลก่อน–หลัง วิเคราะห์สถิติ และแดชบอร์ด 3 ระดับ (นักเรียน / ครู / ผู้บริหาร)

**เผยแพร่** https://sarawut2206.github.io/LIFECRAFT-AI/
**Repo** https://github.com/sarawut2206/LIFECRAFT-AI (public)

**ภายใต้โครงการ** สร้างและส่งเสริมความเป็นพลเมืองดีตามรอยพระยุคลบาทด้านการศึกษาสู่การปฏิบัติ
สำนักงานศึกษาธิการจังหวัดกรุงเทพมหานคร / ศธภ.2

---

## 2. งานที่ต้องทำต่อ เรียงตามความเร่งด่วน

### 🔴 ด่วนที่สุด — Symposium 14 สิงหาคม 2569 (เหลือ 3 วัน)

หนังสือเชิญ ที่ ศธ 0257/ว548 ลงวันที่ 8 ส.ค. 2569 เชิญ **นายศราวุธ พิมศร** โดยระบุชื่อ

| | |
|---|---|
| วัน–เวลา | ศุกร์ที่ 14 ส.ค. 2569 · 08.30–16.30 น. |
| สถานที่ | โรงแรมดิไอเดิล โฮเทล แอนด์ เรสซิเดนซ์ จ.ปทุมธานี |
| กลุ่ม | **กลุ่มที่ 1 สมุทรปราการ + กรุงเทพมหานคร** |
| วิทยากรกลุ่ม | นายเทพฤทธิ์ ยอดใส ผอ.ร.ร.ราชวินิตมัธยม (ผอ.เชี่ยวชาญ) |

**นี่ไม่ใช่การประกวด** แต่คือเวทีแลกเปลี่ยนเพื่อ **ต่อยอดนวัตกรรม · ถอดบทเรียน · สร้างรูปแบบ/แนวทาง**
งานส่งประกวดรอบ 7 ส.ค. ผ่านไปแล้ว

**กำหนดการ**
- 09.30–12.00 เสวนาวิชาการ + นำเสนอรูปแบบ/แนวทาง (แบ่ง 3 กลุ่มจังหวัด)
- 13.00–15.00 Symposium แลกเปลี่ยนเรียนรู้
- 15.00–16.30 สรุปการนำรูปแบบไปปฏิบัติในพื้นที่

**สิ่งที่ยังไม่มีและควรทำ**
- [ ] แผ่นพับ 1 หน้า + QR ให้ครูสแกนลองใช้ในห้องประชุมทันที ← คุ้มที่สุด
- [ ] สคริปต์นำเสนอจับเวลา (ยังไม่ทราบว่าได้กี่นาที ต้องถามผู้จัด)
- [ ] คู่มือการนำไปใช้ระดับโรงเรียน สำหรับผู้บริหารโรงเรียนอื่น

### 🟡 สำคัญแต่ไม่ผูกเดดไลน์

- [ ] **ผู้เชี่ยวชาญ 5 ท่านประเมิน IOC จริง** — ค่าในระบบยังเป็นข้อมูลจำลอง
      ข้อคำถามใหม่ 2 ข้อ (วินัย, ยอมรับความแตกต่าง) ยังไม่ผ่านการตรวจสอบเลย
- [ ] **เก็บข้อมูลจริงให้ครบ** — ปัจจุบัน 6/15 คน (ดูข้อ 5)
- [ ] **ปรับรายงาน 5 บท** ให้ตรงกับระบบปัจจุบัน — รายงานยังเขียนไว้ว่า 7 ด้าน 10 ข้อ
      และยังไม่มี PWA, เช็คอินใจ, ระดับโรงเรียน, พระบรมราโชบาย 4 ด้าน
- [ ] **ภาพหน้าจอในภาคผนวก ค** เป็นเวอร์ชันเก่าก่อนปรับ UI ทั้งหมด
- [ ] **ตรวจบรรณานุกรม 28 รายการ** ยังไม่ได้เปิดต้นฉบับตรวจทีละรายการ

### 🟢 ทางเลือกพัฒนาต่อ

- [x] ~~เพิ่มแบบสังเกตพฤติกรรมโดยครูให้ครบ 9 ด้าน แล้วให้ระบบคำนวณสหสัมพันธ์~~
      **ทำแล้ว 12 ส.ค. 2569** (ดูข้อ 4.3 และ 8) เหลือแค่ให้ครูประเมินข้อมูลจริง
- [ ] เปลี่ยนรหัสครู `SaM2569` ที่เปิดเผยในโค้ดสาธารณะ
- [ ] ให้ครูประเมินแบบสังเกต 9 ด้านกับนักเรียนจริงอย่างน้อย 3 คน
      ระบบจึงจะคำนวณค่า r ในตารางที่ 6 ได้ (ตอนนี้ยังไม่มีข้อมูลจริงเลย)

---

## 3. สถาปัตยกรรมระบบ

```
index.html          ตัวโปรแกรมทั้งหมดในไฟล์เดียว (~230 KB) ไม่มี build step ไม่มี dependency
manifest.webmanifest  PWA manifest ติดตั้งเป็นแอปได้
sw.js               Service Worker ใช้งานออฟไลน์ (VERSION ต้องเพิ่มทุกครั้งที่แก้ไฟล์แคช)
icons/              ไอคอน any + maskable
Code.gs             Apps Script backend เวอร์ชัน 2
```

**เก็บข้อมูล** localStorage ก่อนเสมอ แล้วซิงก์ขึ้น Google Sheet ของโรงเรียนเอง
ไม่มีเซิร์ฟเวอร์กลาง สอดคล้อง PDPA

**รูปแบบคีย์ข้อมูล**

| ประเภท | คีย์ |
|---|---|
| ภารกิจที่ 1 | `lc:<idx>` |
| ภารกิจที่ 2+ | `lc:m<mid>:<idx>` |
| ภารกิจด่วน | `lc:qdef:<code>` / `lc:q:<code>:<idx>` |
| ติดตามการลงมือทำ | `lc:act:<mid>:<idx>` |
| เช็คอินใจ | `lc:pulse:<YYYY-Www>:<idx>` |
| ทะเบียนห้อง | `lc:school:rooms` |
| สรุประดับห้อง | `lc:school:sum:<รหัสห้อง>` |
| ตั้งค่า | `lc:cfg:room` `grade` `roster` `school` `roomcode` `teacher` |

---

## 4. กรอบการวัด (ปรับใหม่ 11 ส.ค. 2569)

### สมรรถนะ 9 ด้าน · แบบวัด 12 ข้อ · คะแนนเต็ม 48

| กลุ่ม | ด้าน (key) |
|---|---|
| ทักษะชีวิต | การจัดการตนเอง `self` · การคิดและแก้ปัญหา `think` · การสื่อสารและทำงานร่วมกัน `comm` |
| พลเมืองดี | ความรับผิดชอบ `resp` · ความซื่อสัตย์ `hon` · การเคารพสิทธิผู้อื่น `resp2` · การคำนึงถึงประโยชน์ส่วนรวม `common` · **วินัยและการเคารพกติกา `disc`** · **การยอมรับความแตกต่าง `divers`** |

`disc` และ `divers` เพิ่มเมื่อ 11 ส.ค. 2569 เพื่อให้ครบตามกรอบพลเมืองดีของไทย
(คุณลักษณะอันพึงประสงค์ 8 ประการ · ค่านิยมหลัก 12 ประการ · จุดเน้นหน้าที่พลเมือง · กรอบ สกศ.)

### ชั้นสังเคราะห์ พระบรมราโชบาย 4 ด้าน (ค่าคงที่ `ROYAL`)

| ด้าน | สมรรถนะที่รองรับ |
|---|---|
| มีทัศนคติที่ถูกต้องต่อบ้านเมือง | `resp2` `divers` `common` |
| มีพื้นฐานชีวิตที่มั่นคง มีคุณธรรม | `hon` `disc` `think` |
| มีงานทำ มีอาชีพ | `self` `resp` `comm` |
| เป็นพลเมืองดี | `common` `resp` `disc` `resp2` |

### 4.3 แบบสังเกตพฤติกรรมของครู 9 ด้าน (`TOBS`) — เพิ่ม 12 ส.ค. 2569

ค่าคงที่ **`TOBS`** คือแบบสังเกตพฤติกรรมที่ครูเป็นผู้ให้ระดับ 1–4 ครบทั้ง 9 ด้าน
คีย์ตรงกับ `DIMS` ทุกตัว เก็บใน `record.teacher.score`

> **สำคัญมากเมื่อแก้โค้ด** ในไฟล์มีรูบริก **สองชุด อย่าสลับกัน**
> - `RUBRICS` 4 ด้าน = **นักเรียนประเมินตนเอง** (ขั้น Opt) คงไว้ 4 ด้านโดยตั้งใจ เพื่อไม่เพิ่มภาระนักเรียน
> - `TOBS` 9 ด้าน = **ครูสังเกตพฤติกรรม** ใช้คู่กับแบบวัดเพื่อหาค่า r

ความเข้ากันได้กับข้อมูลเดิม คีย์ `self` `think` `comm` เหมือนรูบริกเดิมทุกประการ
ระดับที่ครูเคยให้ไว้จึงใช้ต่อได้ทันที ส่วนคีย์ `civic` ของฉบับเดิมถูกแยกเป็น 6 ด้านย่อย
ระบบไม่เดาค่าให้ แต่แสดงเป็นแถว "ค่าจากแบบสังเกตฉบับเดิม" ในรายงานรายบุคคล
**ผลคือข้อมูลจริง 6 คนเดิมจะขึ้นเป็น 3/9 ด้าน ครูต้องให้ระดับอีก 6 ด้านเองก่อนจึงจะคำนวณ r ได้**

### 4.4 ความเที่ยงตรงเชิงสภาพ (Concurrent Validity)

ตารางที่ 6 ในหน้าสถิติ คำนวณสหสัมพันธ์แบบเพียร์สันระหว่าง
คะแนนรายด้านจากแบบวัดของนักเรียน (หลังเรียน) กับระดับที่ครูให้จากแบบสังเกต ทั้งคู่เป็นมาตร 1–4

| | วิธีนับ |
|---|---|
| รายด้าน | ตัดเป็นคู่ นับเฉพาะผู้เรียนที่มีทั้งสองค่าในด้านนั้น |
| ภาพรวม | นับเฉพาะผู้เรียนที่ครูประเมินครบ 9 ด้าน **และ** ทำแบบวัดครบทุกด้าน |

ทดสอบนัยสำคัญด้วย t = r√(df/(1−r²)) เมื่อ df = n−2
ค่า `r` ระดับห้องถูกส่งขึ้นแดชบอร์ดผู้บริหารเป็นคอลัมน์ "ความเที่ยงตรง r" ด้วย
(เป็นค่าสรุประดับห้องเท่านั้น ไม่มีคำตอบรายบุคคล ตาม PDPA เดิม)
จุดเทียบจากงานวิจัย ป.6 คือ r = 0.84

### 4.5 หลักฐานความสำเร็จ 5 ด้าน — เพิ่ม 12 ส.ค. 2569

ความสำเร็จของงานพลเมืองดีไม่ได้วัดจากคะแนนหลังเรียนที่สูงขึ้นอย่างเดียว
หน้า **หลักฐานความสำเร็จ 5 ด้าน** (`openEvid()`) รวบรวมหลักฐานครบทั้ง 5 ด้านจากข้อมูลจริงในระบบ

| ด้าน | แหล่งหลักฐาน | ตัวชี้วัด |
|---|---|---|
| 1 ความรู้และความเข้าใจหน้าที่/สิทธิ | แบบวัดสถานการณ์ 12 ข้อ ก่อน–หลัง | % ถึงระดับสามารถ · t-test |
| 2 คิดวิเคราะห์ตัดสินใจด้วยเหตุผล | ขั้น T และ O + บันทึกสนทนา AI โค้ช | ทางเลือก ≥ 2 พร้อมผู้เสียประโยชน์ · เหตุผลอ้างข้อมูล · การทบทวน · คะแนนด้าน `think` |
| 3 เคารพความแตกต่างและสิทธิผู้อื่น | แบบวัดรายด้าน + แบบสังเกตของครู | `resp2` `divers` + % ที่ครูเห็นระดับ 3 ขึ้นไป |
| 4 รับผิดชอบและจิตสาธารณะ | แบบสังเกตของครู + บทบาทในขั้น G | `resp` `common` + `disc` + % ระบุสิ่งที่ทำจริง |
| 5 ลงมือทำจริง | ขั้น W + **โมดูลติดตามการลงมือทำ** + ครูยืนยัน | % เสนอข้อตกลง · % กลับมารายงาน · % ทำจริง · % ครูยืนยันหลักฐาน |

**กฎการสรุปสถานะรายด้าน** มีตัวชี้วัดใดไม่ผ่าน = ยังไม่ถึงเกณฑ์ · มีตัวชี้วัดใดไม่มีข้อมูล = รอข้อมูล
ด้านจะ "ผ่านเกณฑ์" ได้เมื่อมีหลักฐานครบทุกตัวชี้วัดเท่านั้น
**จงใจไม่ให้ด้าน 3–4 ผ่านด้วยคะแนนที่นักเรียนตอบเองอย่างเดียว** ถ้าครูยังไม่ได้สังเกตพฤติกรรม
เพราะขัดกับหลักของกรอบนี้ที่ให้น้ำหนักหลักฐานพฤติกรรม

### 4.6 โมดูลติดตามการลงมือทำ (Action Follow-up)

เดิมขั้น W เก็บได้แค่ "คำสัญญา" ไม่มีอะไรตามว่าเกิดขึ้นจริงไหม โมดูลนี้ปิดวงจร

- **นักเรียน** หลังทำภารกิจเสร็จ การ์ดบนหน้าแรกจะชวนกลับมารายงานผล
  (ทำแล้ว / ทำได้บางส่วน / ยังไม่ได้ทำ + เล่าสิ่งที่เกิดขึ้น + ผลที่สังเกตได้)
  ถามซ้ำจนกว่าสถานะจะเป็น "ทำแล้ว" ถ้ายังไม่ได้ทำก็ถามว่าติดขัดอะไร
- **ครู** กดยืนยันหรือปฏิเสธหลักฐานได้จากตารางในหน้าหลักฐาน หรือกรอกแทนนักเรียน
- **คีย์ข้อมูล** `lc:act:<mid>:<idx>` ซิงก์ผ่านกลไกเดิม **ไม่ต้องแก้ Apps Script**

> ⚠️ **ข้อมูลเดิม 6 คนทำแบบวัด 10 ข้อ (เต็ม 40) ส่วนฉบับปัจจุบัน 12 ข้อ (เต็ม 48)**
> ระบบตรวจจับและเตือนอัตโนมัติในหน้าสถิติและหน้าครู
> คะแนนรวมเทียบกันไม่ได้ แต่คะแนนรายด้านและพระบรมราโชบายยังเทียบได้ เพราะเป็นค่าเฉลี่ย 1–4

---

## 5. ข้อมูลจริงที่มีอยู่

ภารกิจที่ 1 เงินส่วนรวมของห้อง · ม.4/4 · 15 คน · เก็บเมื่อ 6–7 ส.ค. 2569

| สถานะ | จำนวน |
|---|---|
| ทำครบทั้งก่อนและหลัง | **6 คน** |
| ทำแบบวัดก่อนแล้ว แต่ค้างกลางภารกิจ | 9 คน |

**สถิติเบื้องต้น (n=6)** t = 0.200, df = 5, p = .849 → ยังไม่มีนัยสำคัญ (n น้อยเกินไป)

**ข้อค้นพบสำคัญที่มีค่ากว่าคะแนน** กลไกตรวจสอบความตรงจับได้ 2 กรณีจริง

1. นักเรียนทำแบบวัดก่อนเรียนได้เต็ม 40/40 แต่ตอบเองว่า
   *"เคยเจอ แต่ตอนนั้นทำต่างจากที่เลือกไว้ตอนนี้"*
2. นักเรียนได้เต็ม 40/40 ทั้งก่อนและหลัง แต่ตอบว่า
   *"จะยอมตามเสียงส่วนใหญ่เพราะไม่อยากมีปัญหา"*

เป็นหลักฐานเชิงประจักษ์ว่ากลไก 3 ชั้นทำงานจริง ตรงกับงานวิจัย social desirability bias
**ใช้เล่าในเวที Symposium ได้ดีกว่าตัวเลขคะแนน**

**เหตุผลที่ 9 คนทำไม่จบ** ติดขั้น O ตัดสินใจมากที่สุด (5 คน) เพราะต้องพิมพ์เยอะ
แก้แล้วโดยลดช่องบังคับจาก 26 เหลือ 16 ช่อง

---

## 6. ความสามารถของระบบ

**ฝั่งนักเรียน** ภารกิจ 8 ขั้น UP TO GROW · แบบวัดก่อน–หลัง 12 ข้อ · AI Life Coach (ตั้งคำถาม ไม่ให้คำตอบ) · เช็คอินใจ 60 วินาที/สัปดาห์ · ระบบจำจุดที่ค้างไว้

**ฝั่งครู** Dashboard เรียลไทม์ · วิเคราะห์สถิติอัตโนมัติ (t-test, E.I., Cohen's d, รายด้าน, พระบรมราโชบาย, **สหสัมพันธ์ความเที่ยงตรงเชิงสภาพ**) · **แบบสังเกตพฤติกรรม 9 ด้าน พร้อมคอลัมน์เทียบกับคำตอบของนักเรียนรายคน** · คำนวณ IOC · กลไกตรวจสอบความตรง 3 ชั้น · สัมภาษณ์เชิงกระบวนการคิด · แดชบอร์ดสุขภาวะ · สร้างแผนการสอนและใบงานอัตโนมัติ · โหมดกรอกแทนสำหรับเด็กไม่มีมือถือ · ส่งออก CSV

**ฝั่งผู้บริหาร** แดชบอร์ดระดับโรงเรียน เทียบทุกห้อง · ผลพระบรมราโชบายถ่วงน้ำหนัก · ห้องที่ควรสนับสนุนก่อน
**เห็นเฉพาะค่าสรุประดับห้อง ไม่มีคำตอบรายบุคคล**

**การเข้าถึง** PWA ติดตั้งได้ทั้ง Android/iOS ไม่ผ่าน App Store · ออฟไลน์เต็มรูปแบบ · ปรับขนาดตัวอักษร 85–160% · โหมดคมชัดสูง · ใบงานกระดาษ · ใช้ได้ทุกระดับชั้น

---

## 7. ข้อควรรู้ทางเทคนิค (สำคัญมากเมื่อแก้ไขต่อ)

1. **ไฟล์งานจริงชื่อ `index (30).html`** ในโฟลเดอร์ `โปรแกรม up to grow`
   ตอน push ต้องคัดลอกเป็น `index.html`
2. **ตรวจ syntax ทุกครั้ง** ดึง `<script>` ออกมาแล้ว `node --check`
   และตรวจ inline handler ทุกตัวด้วย `new Function()` เพราะ handler ที่สร้างจาก template
   จะพังตอนคลิกเท่านั้น ไม่พังตอนโหลด
3. **`read_console_messages` เก็บ error ค้างข้ามการรีโหลด** ให้ยืนยันด้วยการเช็กว่า
   สัญลักษณ์ท้ายสคริปต์ (`checkResume`, `scrollToMissions`) ถูกนิยามหรือยัง
4. **ทดสอบหน้าที่เรียกเครือข่าย** ต้อง stub `window.pullAll` และ `window.apiCall` ก่อน
   ไม่งั้น `openTeacher()` จะค้าง
5. **แก้ `sw.js` ต้องเพิ่ม `VERSION`** ไม่งั้นเครื่องที่ติดตั้งแอปแล้วจะไม่ได้ไฟล์ใหม่
6. **`seedDemo()` ไม่ทับข้อมูลจริง** ตรวจ `demo===true` ก่อนเขียนทุกครั้ง (แก้บั๊กนี้แล้ว)
7. **Apps Script cold start ~10–17 วินาที** ครั้งถัดไป 2–3 วินาที
   แนะนำครูเปิดหน้าครูทิ้งไว้ก่อนเข้าคาบเพื่อปลุกเซิร์ฟเวอร์
8. **ยังต้องรัน `migratePulseRows()`** ใน Apps Script หนึ่งครั้ง
   เพื่อย้ายข้อมูลเช็คอินใจเดิมจากชีต `responses` ไปชีต `pulse`
9. **มีรูบริก 2 ชุด อย่าสลับกัน** `RUBRICS` = นักเรียนประเมินตนเอง 4 ด้าน ·
   `TOBS` = ครูสังเกตพฤติกรรม 9 ด้าน (ดูข้อ 4.3) เวลาแก้ให้ดูก่อนว่าจุดนั้นเป็นของนักเรียนหรือของครู
10. **ตรวจสูตรสถิติใหม่ทุกครั้งด้วย `scipy.stats`** `pairedT` และ `pearson` ตรวจแล้วตรงถึงหลักที่ 15
   (`stats.ttest_rel` และ `stats.pearsonr`) ถ้าแก้สูตรต้องรันเทียบซ้ำ
11. **`sw.js` อยู่ที่ `VERSION = 'v6'`** เพิ่มเป็น v7 ในการแก้ครั้งถัดไปที่แตะไฟล์ที่ถูกแคช
12. **สถานะของนักเรียนต้องสอดคล้องกันเสมอ** `phase` ต้องตรงกับ `step` ก่อนเรียก `save()`
    เพราะ `save()` ส่งขึ้นชีตทันที ถ้าบันทึกสถานะกลางคัน ชีตจะเก็บค่าที่เป็นไปไม่ได้ค้างถาวร
    และอย่าเข้าถึง `STEPS[d.step]` ตรง ๆ ให้ใช้ `progressText(d)` ที่กันค่านอกขอบเขตไว้แล้ว
13. **หน้าที่ขึ้นข้อความ "กำลังโหลด…" ต้องมีตัวดักข้อผิดพลาด** ไม่งั้นข้อผิดพลาดตอนวาดหน้า
    จะกลายเป็นข้อความค้างที่ทำให้เข้าใจผิดว่าต่อเครือข่ายไม่ได้ (`openTeacher` ทำแล้ว)

---

## 8. ประวัติการพัฒนา (ล่าสุดอยู่บนสุด)

| วันที่ | สิ่งที่ทำ |
|---|---|
| 12 ส.ค. | **แก้บั๊กหน้าครูค้างที่ "กำลังดึงข้อมูลจาก Google Sheet…"** — ไม่ใช่ปัญหาเครือข่าย แต่ `STEPS[d.step]` อ่านค่านอกขอบเขตจากข้อมูลจริงที่ `step=8` คู่กับ `phase='mission'` · แก้ต้นเหตุที่ `nextStep()` · เพิ่ม `progressText()` · กู้สถานะอัตโนมัติใน `route()` · ครอบ `openTeacher` ด้วยตัวดักข้อผิดพลาด |
| 12 ส.ค. | เพิ่มหน้าหลักฐานความสำเร็จ 5 ด้าน (`openEvid`) · โมดูลติดตามการลงมือทำ (`openAct` คีย์ `lc:act:`) · การ์ดชวนรายงานผลบนหน้าแรก · แก้บั๊ก `checkResume` ที่ไม่ล้างกล่องเดิม ทำให้การ์ดค้าง |
| 12 ส.ค. | เพิ่ม `TOBS` แบบสังเกตพฤติกรรมของครู 9 ด้าน · ตารางที่ 6 ความเที่ยงตรงเชิงสภาพ (Pearson r ตรวจกับ scipy แล้ว) · คอลัมน์ r ในแดชบอร์ดผู้บริหาร · แบบบันทึกการสังเกตแบบกระดาษ · แก้บั๊ก pill โหมดไม่มีมือถือที่ยังค้างเลข 10 |
| 11 ส.ค. | ปรับกรอบเป็น 9 ด้าน 12 ข้อ · เพิ่มพระบรมราโชบาย 4 ด้าน · เพิ่มแดชบอร์ดผู้บริหารระดับโรงเรียน |
| 11 ส.ค. | ปรับ backend Apps Script เวอร์ชัน 2 (ค้นหาด้วยคอลัมน์ key · แยกชีต pulse · saveMany · prefix filter) |
| 10 ส.ค. | เพิ่มโมดูลเช็คอินใจ · Pulse (ตรวจสุขภาวะรายสัปดาห์) |
| 10 ส.ค. | ทำเป็น PWA ติดตั้งเป็นแอปได้ · ออฟไลน์เต็มรูปแบบ |
| 10 ส.ค. | ลดช่องบังคับกรอก 26 → 16 · ขยายตัวอักษร · แก้บั๊กคอนทราสต์ |
| 7–9 ส.ค. | รายงาน 5 บท · ภาพหน้าจอ · สถิติ · IOC · แผนการสอน · โหมดไม่มีมือถือ |

---

## 9. ไฟล์สำคัญในเครื่อง

โฟลเดอร์ `C:\Users\KruSam\Downloads\โปรแกรม up to grow\`

| ไฟล์ | คำอธิบาย |
|---|---|
| `index (30).html` | **ไฟล์งานจริง** แก้ที่นี่ |
| `Code_ใหม่.gs` | Apps Script เวอร์ชัน 2 (deploy แล้ว) |
| `รายงานนวัตกรรม_UPTOGROW_ไฮไลท์ส่วนที่เพิ่ม.docx` | รายงาน 5 บท 46 หน้า (ส่วนที่เพิ่มไฮไลท์เหลือง) |
| `ภาพหน้าจอโปรแกรม/` | ภาพหน้าจอ 16 ภาพ (เวอร์ชันเก่า) |
| `pwa/` | manifest, sw.js, icons, README |
| `หนังสือเชิญ.pdf` (ใน Downloads) | หนังสือเชิญ Symposium 14 ส.ค. |
